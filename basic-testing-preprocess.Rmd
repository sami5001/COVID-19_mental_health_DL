---
title: "Basic Testing"
author: "Sami Adnan"
date: "6/1/2020"
output:
  html_document:
    code_folding: show
    highlight: breezedark
    theme: darkly
    toc: yes
    toc_float:
      collapsed: no
      smooth_scroll: yes
  pdf_document:
    toc: yes
---



- Loading packages, auto installation if needed
```{r load packages, results='hide', message=FALSE, warning=FALSE}
# creating requiredPkg function: install and load multiple R packages.
# for checking to see if required packages are installed.
# install if needed, then load into the R session.

requiredPkg <- function(pkg){
new.pkg <- pkg[!(pkg %in% installed.packages()[, "Package"])]
if (length(new.pkg)) 
    install.packages(new.pkg, dependencies = TRUE)
sapply(pkg, require, character.only = TRUE)
}

# applying the function
packages <- c("ggplot2", "foreign", "qwraps2")
requiredPkg(packages)

```

### Import data
- Setting directory
- Loading the dataset 
```{r load data, results='hide', message=FALSE, warning=FALSE}
#setting working directory
(WD <- getwd())
if (!is.null(WD)) setwd(WD)
# setwd("")
dat <- read.table("feasibility/", to.data.frame = TRUE)
```